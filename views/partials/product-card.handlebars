
  <a href="/product/{{id}}">
    <div class="item-card">
        <img src="/images/{{filename}}" alt="{{description}}">
    </div>
    <div class= "product-info" >
        <span>{{name}}</span>
        <span>{{price}}</span>
    </div>
  </a>
  <div>
     <button class="custom-button"><i>-</i></button>
     <span>{{qty.count}}</span>
     <button class="custom-button"><i>+</i></button>
  </div>
