<div class="container">
  <div class="notification is-primary">
    {{#if cartItems}}

    {{#grouped_each 4 cartItems}}
    <div class="row cart-row">
      {{#each this}}
      <div class="col-md-3 col-sm-6 product">
        <div class="thumbnail">
          <img src="{{Product.img}}" alt="{{Product.name}}">
          </a>
          <div class="caption">
            <h3>{{Product.name}}</h3>
            <p class="price">${{Product.price}}</p>
            <button><i> plus</i></button>
            <span>{{qty.count}}</span>
            <button><i>minus</i></button>
            <p> {{product.total}}</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>